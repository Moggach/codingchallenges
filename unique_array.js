//Write a function uniq(arr). It should return a new array without any duplicate values. It should not change the original array.
//You must use forEach, includes and a stack function

function uniq(arr) {
  return arr;
}
